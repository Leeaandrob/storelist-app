(function () {
    'use strict';
    angular
        .module('storeListApp.dataStored', [])
            .factory('DATA', function($ionicPlatform, $rootScope, $timeout, $localStorage) {
            });
}());